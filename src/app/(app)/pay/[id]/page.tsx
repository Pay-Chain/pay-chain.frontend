import { PaymentView } from '@/presentation/view/payment/PaymentView';

export const metadata = {
  title: 'Complete Payment | PaymentKita',
  description: 'Secure cross-chain payment processing gateway.',
};

export default function PayPage() {
  return <PaymentView />;
}

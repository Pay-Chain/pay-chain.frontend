import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove rewrites - using Proxy API route instead
  // Configure experimental features if needed
};

export default nextConfig;

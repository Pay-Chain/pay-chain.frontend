# Backend URL for server-side direct calls (SSR)
BACKEND_URL=http://localhost:8080

# WalletConnect Project ID for AppKit (Sign in with Wallet)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here
